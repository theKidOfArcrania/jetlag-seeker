{
  "name": "jetlag_seek",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Mobile-first seeker location-narrowing webapp for Jet Lag: Hide + Seek (Seattle)",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "build:data": "~/git/jetlag_mapper/.venv/bin/python scripts/build_dataset.py"
  },
  "devDependencies": {
    "@types/leaflet": "^1.9.21",
    "@types/node": "^26.1.1",
    "puppeteer": "^25.3.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "leaflet": "^1.9.4"
  }
}
